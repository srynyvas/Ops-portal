# FastAPI Configuration
HOST=0.0.0.0
PORT=8080
RELOAD=True

# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=ops_portal

# CORS Settings
CORS_ORIGINS=["http://localhost:3000", "http://localhost:3001", "http://localhost:3002", "http://localhost:5173"]

# API Settings
API_VERSION=v1
API_PREFIX=/api

## GitHub Enterprise Configuration Example

# GitHub Type: Set to 'enterprise' for GitHub Enterprise Server
GITHUB_TYPE=enterprise

# GitHub Enterprise Server URL (without /api/v3)
# Examples:
# - https://github.yourcompany.com
# - https://git.enterprise.local
# - https://github-enterprise.company.internal
GITHUB_ENTERPRISE_URL=https://github.yourcompany.com

# GitHub Enterprise Credentials
# Token should have 'repo' scope at minimum
GITHUB_TOKEN=your_enterprise_personal_access_token_here
GITHUB_OWNER=your_organization_or_username
GITHUB_REPO=your_repository_name

# Note: For GitHub Enterprise, ensure:
# 1. The token is generated from your enterprise server (not github.com)
# 2. The enterprise URL is accessible from your backend server
# 3. The token has appropriate permissions (repo scope)
# 4. SSL certificates are properly configured if using HTTPS